# =============================================================================
# SHARED ENVIRONMENT VARIABLES
# =============================================================================
# CRITICAL: This file serves as the source of truth for all environment variables.
# Copy this file to .env and fill in the actual values.
# The .env file is gitignored and will NEVER be committed.

# =============================================================================
# AUTHENTICATION (CRITICAL - Must Match Between Frontend and Backend!)
# =============================================================================

# JWT signing/verification secret
# MUST be identical in both frontend and backend for authentication to work
# Minimum 32 characters recommended for security
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-at-least-32-characters-long

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Backend API base URL
# Default for local development
NEXT_PUBLIC_API_URL=http://localhost:8000

# Node environment
# Options: development | production | test
NODE_ENV=development

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Neon PostgreSQL connection string
# Get this from: https://neon.tech dashboard
# Format: postgresql://username:password@hostname/database?sslmode=require
DATABASE_URL=postgresql://username:password@hostname.neon.tech/database?sslmode=require

# CORS allowed origins
# Comma-separated list for production, single origin for development
CORS_ORIGINS=http://localhost:3000

# Backend server port
# Default: 8000
PORT=8000

# Debug mode (enables SQL query logging)
# Options: true | false
DEBUG=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Generate BETTER_AUTH_SECRET: openssl rand -base64 32
# 3. Create Neon PostgreSQL database: https://neon.tech
# 4. Copy DATABASE_URL from Neon dashboard
# 5. Ensure BETTER_AUTH_SECRET is IDENTICAL in both frontend and backend sections
# 6. Start services: docker-compose up
#
# =============================================================================
# SECURITY WARNINGS
# =============================================================================
#
# ⚠️  NEVER commit .env file to version control
# ⚠️  BETTER_AUTH_SECRET must match between frontend and backend
# ⚠️  Use strong, random secrets (minimum 32 characters)
# ⚠️  DATABASE_URL contains credentials - keep it secret
# ⚠️  In production, use secure secret management (not .env files)
#
# =============================================================================
