# =============================================================================
# BACKEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in the actual values.
# NEVER commit .env to version control.

# =============================================================================
# CRITICAL: JWT SECRET (MUST MATCH FRONTEND!)
# =============================================================================

# JWT signing/verification secret
# ⚠️ CRITICAL: This MUST be IDENTICAL to the frontend BETTER_AUTH_SECRET
# Minimum 32 characters recommended for security
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-at-least-32-characters-long

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Neon PostgreSQL connection string
# Get this from: https://neon.tech dashboard
# Format: postgresql://username:password@hostname.neon.tech/database?sslmode=require
DATABASE_URL=postgresql://username:password@hostname.neon.tech/database?sslmode=require

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed frontend origins (comma-separated for multiple)
# Local development: http://localhost:3000
# Production: https://your-frontend-domain.com
CORS_ORIGINS=http://localhost:3000

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port
# Default: 800
PORT=8000

# Debug mode (enables SQL query logging and detailed errors)
# Options: true | false
# ⚠️ Set to false in production
DEBUG=false

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
# 2. Generate BETTER_AUTH_SECRET: openssl rand -base64 32
# 3. Paste the SAME secret in frontend/.env.example
# 4. Create Neon PostgreSQL database: https://neon.tech
# 5. Copy DATABASE_URL from Neon dashboard
# 6. Update CORS_ORIGINS to match your frontend URL
# 7. Start backend: uvicorn app.main:app --reload
#
# =============================================================================
# SECURITY WARNINGS
# =============================================================================
#
# ⚠️  BETTER_AUTH_SECRET must be IDENTICAL in frontend and backend
# ⚠️  NEVER commit .env to version control
# ⚠️  DATABASE_URL contains credentials - keep it secret
# ⚠️  Use strong, random secrets (minimum 32 characters)
# ⚠️  Set DEBUG=false in production
# ⚠️  Restrict CORS_ORIGINS to known frontend domains only
#
# =============================================================================
# NOTES
# =============================================================================
#
# - All environment variables are server-side only
# - DATABASE_URL must include ?sslmode=require for Neon
# - CORS_ORIGINS should match the exact frontend URL (with protocol and port)
# - Refer to root .env.example as the source of truth for all variables
#
# =============================================================================
